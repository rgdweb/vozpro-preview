server {
    listen 80;
    server_name api.sorteiomax.com.br;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl;
    server_name api.sorteiomax.com.br;

    ssl_certificate /etc/letsencrypt/live/api.sorteiomax.com.br/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.sorteiomax.com.br/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    root /var/www/omnivoice;
    index index.php index.html;
    client_max_body_size 500M;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/run/php/php8.3-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_read_timeout 600;
    }

    location /audios/ {
        alias /var/www/omnivoice/audios/;
        autoindex on;
        autoindex_format html;
        expires 30d;
        add_header Cache-Control "public";
    }
}

server {
    listen 80 default_server;
    server_name _;
    root /var/www/omnivoice;
    index index.php index.html;
    client_max_body_size 500M;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/run/php/php8.3-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_read_timeout 600;
    }

    location /audios/ {
        alias /var/www/omnivoice/audios/;
        autoindex on;
        autoindex_format html;
        expires 30d;
        add_header Cache-Control "public";
    }
}

server {
    server_name api.cvmnews.com.br;
    root /var/www/omnivoice;
    index index.php index.html;
    client_max_body_size 500M;
    add_header Access-Control-Allow-Origin * always;
    add_header Access-Control-Allow-Methods "GET, POST, OPTIONS" always;
    add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Generate-Token" always;
    if ($request_method = OPTIONS) {
        return 204;
    }


    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/run/php/php8.3-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_read_timeout 600;
    }

    listen 443 ssl;
    ssl_certificate /etc/letsencrypt/live/api.cvmnews.com.br/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.cvmnews.com.br/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
}

server {
    if ($host = api.cvmnews.com.br) {
        return 301 https://$host$request_uri;
    }

    listen 80;
    server_name api.cvmnews.com.br;
    return 404;
}

===
===END===
